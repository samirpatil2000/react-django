import React, {Component, componet} from "react";
import { render } from "react-dom";

export default class CreateRoomPage extends Component{
    constructor(props) {
        super(props);
    }

    render() {
        return <h2>Create Room Page</h2>
    }
}