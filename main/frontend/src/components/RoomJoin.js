import React, {Component, componet} from "react";
import { render } from "react-dom";

export default class RoomJoin extends Component{
    constructor(props) {
        super(props);
    }

    render() {
        return <h2>Join Room Page</h2>
    }
}